# AuraOS dedicated Supabase database
SUPABASE_PROJECT_REF=limpprtlwtlvfwezkulq
SUPABASE_URL=https://limpprtlwtlvfwezkulq.supabase.co

# Server-side database connection only. Do NOT commit a real password.
# Prefer Supavisor Session Pooler on IPv4-only hosts such as GitHub Actions/Render.
DATABASE_URL=postgresql://postgres.limpprtlwtlvfwezkulq:[PASSWORD]@[SESSION_POOLER_HOST]:5432/postgres?sslmode=require

NODE_ENV=production
PORT=3000
JWT_SECRET=[GENERATE_AT_LEAST_32_RANDOM_CHARACTERS]
JWT_REFRESH_SECRET=[GENERATE_A_DIFFERENT_32_PLUS_CHARACTER_SECRET]
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

PAYMENT_GATEWAY=none
APP_URL=https://premieros.github.io/55/auraos-preview/
CORS_ORIGIN=https://premieros.github.io

# Optional
REDIS_URL=
SUPER_ADMIN_EMAILS=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
